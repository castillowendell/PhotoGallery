import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'wendell.castillo.photogallery',
  appName: 'Photo-Gallery',
  webDir: 'dist'
};

export default config;
